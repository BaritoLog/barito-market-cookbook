[Unit]
Description=systemd script for running <%= @app_name %>

[Service]
Restart=on-failure
RestartSec=2
StartLimitInterval=50
StartLimitBurst=10
EnvironmentFile=-/etc/default/<%=@app_name%>.conf
ExecStart="puma -p 8080"
User=<%= @user %>

[Install]
WantedBy=multi-user.target
