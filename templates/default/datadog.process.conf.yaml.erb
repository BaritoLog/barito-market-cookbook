init_config:

instances:
  - name: <%= @app_name %>
    search_string: ['<%= @app_search_string %>']
    tags:
      <% @datadog_tags.each do |tag| %>
      - <%= tag %>
      <% end %>
    ignore_denied_access: True
