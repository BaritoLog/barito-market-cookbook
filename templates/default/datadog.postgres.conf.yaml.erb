init_config:

instances:
  - host: localhost
    port: 5432
    username: <%= @db_username %>
    password: <%= @db_password %>
    dbname: <%= @db_name %>
    tags:
      <% @datadog_tags.each do |tag| %>
      - <%= tag %>
      <% end %>
